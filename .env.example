# Optional overrides for the default local Docker stack (docker compose up).
# Copy to .env in this directory. Not used by docker-compose.sqlite.yml.

POSTGRES_PASSWORD=alma
JWT_SECRET=change-me-to-a-long-random-string
ATTORNEY_PASSWORD=changeme
